<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/app_padding"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/activityMain_btnToast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activityMain_btnToast_title" />

    <TextView
        android:id="@+id/activityMain_tvCount"
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:text="@string/activityMain_tvCount_text"
        android:textSize="@dimen/activityMain_tvCount_textSize"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@color/teal_200" />

    <Button
        android:id="@+id/activityMain_btnCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activityMain_btnCount_title" />

</LinearLayout>